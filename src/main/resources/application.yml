server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring-vue-first?serverTimezone=GMT%2b8
    username: root
    password: qwer1234
mybatis:
  mapper-locations: classpath:mapper/*.xml  #????mybatis?xml??
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl